package planarizationTest;

/*Generated by MPS */

import junit.framework.TestCase;
import jetbrains.mps.graphLayout.graph.Graph;
import visualization.GraphIO;
import jetbrains.mps.graphLayout.planarGraph.EmbeddedGraph;
import jetbrains.mps.graphLayout.planarization.MultigraphEmbeddingFinder;
import jetbrains.mps.graphLayout.planarization.ShortestPathEmbeddingFinder;
import jetbrains.mps.graphLayout.planarization.PQPlanarizationFinder;

public class MultigraphEmbeddingFinder_Test extends TestCase {
  public void test_test1() throws Exception {
    String graphString = "3 5  0 1  1 2  2 0  2 0  1 1";
    Graph graph = GraphIO.scanGraph(graphString);
    this.test(graph);
  }

  public void test_test2() throws Exception {
    String graphString = "2 2  0 1  0 1";
    Graph graph = GraphIO.scanGraph(graphString);
    this.test(graph);
  }

  public void test(Graph graph) {
    EmbeddedGraph embeddedGraph = new MultigraphEmbeddingFinder(new ShortestPathEmbeddingFinder(new PQPlanarizationFinder())).find(graph);
    CheckEmbeddedGraph.checkEmbeddedGraph(embeddedGraph, false);
    CheckEmbeddedGraph.checkFull(embeddedGraph);
    System.out.println(embeddedGraph);
  }
}
