package jetbrains.mps.platform.conf.structure;

/*Generated by MPS */

import java.util.List;
import jetbrains.mps.internal.collections.runtime.ListSequence;
import jetbrains.mps.internal.collections.runtime.backports.LinkedList;

public enum GroupPosition {
  FIRST("FIRST", "first"),
  LAST("LAST", "last"),
  BEFORE("BEFORE", "before"),
  AFTER("AFTER", "after");

  private String myName;
  private String myValue;

  GroupPosition(String name, String value) {
    this.myName = name;
    this.myValue = value;
  }

  public String getName() {
    return this.myName;
  }

  public String getValueAsString() {
    return this.myValue;
  }

  public String getValue() {
    return this.myValue;
  }

  public static List<GroupPosition> getConstants() {
    List<GroupPosition> list = ListSequence.<GroupPosition>fromList(new LinkedList<GroupPosition>());
    ListSequence.<GroupPosition>fromList(list).addElement(GroupPosition.FIRST);
    ListSequence.<GroupPosition>fromList(list).addElement(GroupPosition.LAST);
    ListSequence.<GroupPosition>fromList(list).addElement(GroupPosition.BEFORE);
    ListSequence.<GroupPosition>fromList(list).addElement(GroupPosition.AFTER);
    return list;
  }

  public static GroupPosition getDefault() {
    return GroupPosition.FIRST;
  }

  public static GroupPosition parseValue(String value) {
    if (value == null) {
      return GroupPosition.getDefault();
    }
    if (value.equals(GroupPosition.FIRST.getValueAsString())) {
      return GroupPosition.FIRST;
    }
    if (value.equals(GroupPosition.LAST.getValueAsString())) {
      return GroupPosition.LAST;
    }
    if (value.equals(GroupPosition.BEFORE.getValueAsString())) {
      return GroupPosition.BEFORE;
    }
    if (value.equals(GroupPosition.AFTER.getValueAsString())) {
      return GroupPosition.AFTER;
    }
    return GroupPosition.getDefault();
  }
}
