package jetbrains.mps.ide.migration.check;

/*Generated by MPS */

import org.jetbrains.mps.openapi.module.SModule;
import org.jetbrains.mps.openapi.language.SLanguage;

public class MissingMigrationProblem extends Problem<SModule> {
  private SLanguage myLanguage;

  public MissingMigrationProblem(SModule module, SLanguage language) {
    super(module);
    myLanguage = language;
  }

  public String getMessage() {
    return "Module " + getReason().getModuleName() + " uses language " + myLanguage.getQualifiedName() + " with version " + getReason().getUsedLanguageVersion(myLanguage) + " while current version is " + myLanguage.getLanguageVersion() + ". Migrations are missing for this language";
  }

  public String getCategory() {
    return "Missing migrations";
  }
}
