package jetbrains.mps.vcs.mergehints.structure;

/*Generated by MPS */

import jetbrains.mps.smodel.PropertySupport;
import java.util.Iterator;
import jetbrains.mps.internal.collections.runtime.ListSequence;

public class Strategy_PropertySupport extends PropertySupport {
  public boolean canSetValue(String value) {
    if (value == null) {
      return true;
    }
    Iterator<Strategy> constants = ListSequence.fromList(Strategy.getConstants()).iterator();
    while (constants.hasNext()) {
      Strategy constant = constants.next();
      if (value.equals(constant.getName())) {
        return true;
      }
    }
    return false;
  }
  public String toInternalValue(String value) {
    if (value == null) {
      return null;
    }
    Iterator<Strategy> constants = ListSequence.fromList(Strategy.getConstants()).iterator();
    while (constants.hasNext()) {
      Strategy constant = constants.next();
      if (value.equals(constant.getName())) {
        return constant.getValueAsString();
      }
    }
    return null;
  }
  public String fromInternalValue(String value) {
    Strategy constant = Strategy.parseValue(value);
    if (constant != null) {
      return constant.getName();
    }
    return "";
  }
}
