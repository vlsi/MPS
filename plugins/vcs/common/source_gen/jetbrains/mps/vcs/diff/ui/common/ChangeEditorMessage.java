package jetbrains.mps.vcs.diff.ui.common;

/*Generated by MPS */

import jetbrains.mps.nodeEditor.messageTargets.EditorMessageWithTarget;
import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
import jetbrains.mps.vcs.diff.changes.ModelChange;
import jetbrains.mps.smodel.SNode;
import jetbrains.mps.errors.messageTargets.MessageTarget;
import jetbrains.mps.nodeEditor.EditorMessageOwner;
import jetbrains.mps.errors.MessageStatus;
import java.awt.Color;
import jetbrains.mps.vcs.diff.changes.ChangeType;
import jetbrains.mps.nodeEditor.cells.EditorCell;
import jetbrains.mps.errors.messageTargets.MessageTargetEnum;
import jetbrains.mps.nodeEditor.cells.EditorCell_Collection;
import java.awt.Graphics;
import jetbrains.mps.nodeEditor.EditorComponent;
import java.awt.Rectangle;
import jetbrains.mps.baseLanguage.closures.runtime.Wrappers;
import jetbrains.mps.smodel.ModelAccess;
import jetbrains.mps.lang.smodel.generator.smodelAdapter.SNodeOperations;
import jetbrains.mps.nodeEditor.messageTargets.CellFinder;
import jetbrains.mps.nodeEditor.cells.PropertyAccessor;
import jetbrains.mps.nodeEditor.cells.EditorCell_Property;
import jetbrains.mps.nodeEditor.EditorMessage;
import jetbrains.mps.internal.collections.runtime.ListSequence;
import java.util.List;
import jetbrains.mps.internal.collections.runtime.IWhereFilter;
import jetbrains.mps.errors.messageTargets.DeletedNodeMessageTarget;
import jetbrains.mps.ide.util.ColorAndGraphicsUtil;
import jetbrains.mps.nodeEditor.inspector.InspectorEditorComponent;
import jetbrains.mps.baseLanguage.closures.runtime._FunctionTypes;
import java.util.Iterator;
import jetbrains.mps.baseLanguage.closures.runtime.YieldingIterator;
import jetbrains.mps.internal.collections.runtime.Sequence;
import jetbrains.mps.nodeEditor.cellLayout.CellLayout_Vertical;
import jetbrains.mps.nodeEditor.style.StyleAttributes;
import org.jetbrains.annotations.NotNull;
import jetbrains.mps.nodeEditor.cells.ModelAccessor;

public class ChangeEditorMessage extends EditorMessageWithTarget {
  private static final String NAME_PROPERTY = "name";
  protected static Log log = LogFactory.getLog(ChangeEditorMessage.class);
  private ModelChange myChange;
  private ChangeEditorMessage.ConflictChecker myConflictsChecker;
  private boolean myHighlighted;

  protected ChangeEditorMessage(SNode node, MessageTarget target, EditorMessageOwner owner, ModelChange change, ChangeEditorMessage.ConflictChecker conflictChecker, boolean highlighted) {
    super(node, MessageStatus.OK, target, null, "", owner);
    myChange = change;
    myConflictsChecker = conflictChecker;
    myHighlighted = highlighted;
  }

  public boolean isConflicted() {
    return myConflictsChecker != null && myConflictsChecker.isChangeConflicted(myChange);
  }

  @Override
  public boolean showInGutter() {
    return false;
  }

  @Override
  public Color getColor() {
    return ChangeColors.get((isConflicted() ?
      ChangeType.CONFLICTED :
      myChange.getType()
    ));
  }

  public ModelChange getChange() {
    return myChange;
  }

  @Override
  public boolean isBackground() {
    return true;
  }

  private boolean isDirectCell(EditorCell cell) {
    // Return true if and only if this cell should be painted, not only set in frame 
    if (cell == null) {
      return false;
    }
    if (myMessageTarget.getTarget() == MessageTargetEnum.NODE) {
      return getNode() == cell.getSNode();
    } else {
      return !(cell instanceof EditorCell_Collection) && isNameCell(cell) == (myMessageTarget.getTarget() == MessageTargetEnum.PROPERTY && NAME_PROPERTY.equals(myMessageTarget.getRole()));
    }
  }

  @Override
  public void paint(Graphics graphics, EditorComponent editor, EditorCell cell) {
    if (!(myHighlighted)) {
      return;
    }
    if (isDirectCell(cell)) {
      cell.paintSelection(graphics, getColor(), false);
      repaintConflictedMessages(graphics, cell);
    } else {
      if (myMessageTarget.getTarget() == MessageTargetEnum.DELETED_CHILD) {
        drawDeletedChild(graphics, cell);
      } else {
        Rectangle bounds = (isIndirectRoot(editor) ?
          getFirstPseudoLineBounds(editor) :
          cell.getBounds()
        );
        graphics.setColor(ChangeColors.get((isConflicted() ?
          ChangeType.CONFLICTED :
          ChangeType.CHANGE
        )));
        graphics.drawRect(bounds.x + 1, bounds.y + 1, bounds.width - 2, bounds.height - 2);
      }
    }
  }

  @Override
  public EditorCell getCell(final EditorComponent editor) {
    final Wrappers._T<EditorCell> cell = new Wrappers._T<EditorCell>(super.getCell(editor));
    if (check_myu41h_a0b0h(cell.value) && !(isDirectCell(cell.value))) {
      ModelAccess.instance().runReadAction(new Runnable() {
        public void run() {
          SNode node = getNode();
          if (SNodeOperations.isInstanceOf(node, "jetbrains.mps.lang.core.structure.INamedConcept")) {
            cell.value = CellFinder.getCellForProperty(editor, node, NAME_PROPERTY);
          }
        }
      });
    }
    return cell.value;
  }

  @Override
  public boolean acceptCell(EditorCell cell, EditorComponent component) {
    return isNameCell(cell) && !(isDirectCell(cell)) && check_myu41h_a0a0a8(super.getCell(component)) || super.acceptCell(cell, component);
  }

  private boolean isNameCell(EditorCell cell) {
    PropertyAccessor pa = as_myu41h_a0a0a9(check_myu41h_a0a0a9((as_myu41h_a0a0a0a0a9(cell, EditorCell_Property.class))), PropertyAccessor.class);
    return getNode() == check_myu41h_a0a1a9(pa) && NAME_PROPERTY.equals(check_myu41h_a0a0b0j(pa));
  }

  private void repaintConflictedMessages(Graphics graphics, EditorCell cell) {
    // This is a workaround for case when any change message is going to be painted over 
    // "conflicted" red frame. In this case, we repaint conflicted red frame again 
    EditorCell_Collection parent = cell.getParent();
    if (parent != null && parent.getChildCount() == 1) {
      EditorMessage messageToRepaint = ListSequence.fromList(((List<EditorMessage>) parent.getMessages())).findFirst(new IWhereFilter<EditorMessage>() {
        public boolean accept(EditorMessage m) {
          return m instanceof ChangeEditorMessage && ((ChangeEditorMessage) m).isConflicted();
        }
      });
      if (messageToRepaint != null) {
        messageToRepaint.paint(graphics, cell.getEditor(), parent);
      }
    }
  }

  private void drawDeletedChild(Graphics graphics, EditorCell cell) {
    Rectangle bounds = cell.getBounds();
    if (myMessageTarget.getRole().equals(cell.getRole())) {
      int index = ((DeletedNodeMessageTarget) myMessageTarget).getNextChildIndex();
      if (index != -1) {
        EditorCell_Collection collectionCell = (EditorCell_Collection) cell;

        if (hasChildrenWithDifferentNode(cell)) {
          int cellIndex = getChildCellIndex(collectionCell, index);
          if (isVertical(collectionCell)) {
            drawHorizontalLine(graphics, collectionCell, cellIndex);
          } else {
            drawVerticalLineWithArrows(graphics, collectionCell, cellIndex);
          }
        } else {
          cell.paintSelection(graphics, getColor(), false);
        }
      }
    } else {
      graphics.setColor(getColor());
      graphics.drawLine(bounds.x, bounds.y, bounds.x + bounds.width, bounds.y);
    }
  }

  private void drawHorizontalLine(Graphics graphics, EditorCell_Collection collectionCell, int cellIndex) {
    Rectangle bounds = collectionCell.getBounds();
    int y;
    if (-1 < cellIndex && cellIndex < collectionCell.getChildCount()) {
      y = collectionCell.getChildAt(cellIndex).getY();
    } else {
      y = ((int) collectionCell.getChildAt(collectionCell.getChildCount() - 1).getBounds().getMaxY());
    }
    graphics.setColor(getColor());
    graphics.drawLine((int) bounds.getMinX(), y, (int) bounds.getMaxX(), y);
  }

  private void drawVerticalLineWithArrows(Graphics graphics, EditorCell_Collection collectionCell, int cellIndex) {
    int x;
    Rectangle childCellBounds;
    if (-1 < cellIndex && cellIndex < collectionCell.getChildCount()) {
      childCellBounds = collectionCell.getCellAt(cellIndex).getBounds();
      x = childCellBounds.x;
    } else {
      childCellBounds = collectionCell.getCellAt(collectionCell.getChildCount() - 1).getBounds();
      x = (int) childCellBounds.getMaxX();
    }
    int y1 = childCellBounds.y;
    int y2 = (int) childCellBounds.getMaxY();

    graphics.setColor(getColor());
    graphics.drawLine(x, y1, x, y2);
    graphics.fillPolygon(new int[]{x, x - 3, x + 3}, new int[]{y1 - 2, y1 - 5, y1 - 5}, 3);
    graphics.fillPolygon(new int[]{x, x - 3, x + 3}, new int[]{y2 + 2, y2 + 5, y2 + 5}, 3);

    graphics.setColor(ColorAndGraphicsUtil.brightenColor(getColor(), 0.8f));
    graphics.drawPolygon(new int[]{x, x - 3, x + 3}, new int[]{y1 - 2, y1 - 5, y1 - 5}, 3);
    graphics.drawPolygon(new int[]{x, x - 3, x + 3}, new int[]{y2 + 2, y2 + 5, y2 + 5}, 3);
  }

  private boolean isIndirectRoot(EditorComponent editor) {
    return !(isDirectCell(getCell(editor))) && check_myu41h_a0a0a0o(getNode(), this) == null && !(editor instanceof InspectorEditorComponent);
  }

  private Rectangle getFirstPseudoLineBounds(EditorComponent editor) {
    Iterable<EditorCell> leafCells = new _FunctionTypes._return_P1_E0<Iterable<EditorCell>, EditorCell>() {
      public Iterable<EditorCell> invoke(final EditorCell cell) {
        return new Iterable<EditorCell>() {
          public Iterator<EditorCell> iterator() {
            return new YieldingIterator<EditorCell>() {
              private int __CP__ = 0;
              private EditorCell _4_child;
              private Iterator<EditorCell> _4_child_it;
              private EditorCell _7__yield_myu41h_a0a0a0a0a0a51;
              private Iterator<EditorCell> _7__yield_myu41h_a0a0a0a0a0a51_it;

              protected boolean moveToNext() {
__loop__:
                do {
__switch__:
                  switch (this.__CP__) {
                    case -1:
                      assert false : "Internal error";
                      return false;
                    case 4:
                      this._4_child_it = Sequence.fromIterable((EditorCell_Collection) cell).iterator();
                    case 5:
                      if (!(this._4_child_it.hasNext())) {
                        this.__CP__ = 1;
                        break;
                      }
                      this._4_child = this._4_child_it.next();
                      this.__CP__ = 6;
                      break;
                    case 7:
                      this._7__yield_myu41h_a0a0a0a0a0a51_it = Sequence.fromIterable(invoke(_4_child)).iterator();
                    case 8:
                      if (!(this._7__yield_myu41h_a0a0a0a0a0a51_it.hasNext())) {
                        this.__CP__ = 5;
                        break;
                      }
                      this._7__yield_myu41h_a0a0a0a0a0a51 = this._7__yield_myu41h_a0a0a0a0a0a51_it.next();
                      this.__CP__ = 9;
                      break;
                    case 2:
                      if (cell instanceof EditorCell_Collection) {
                        this.__CP__ = 3;
                        break;
                      }
                      this.__CP__ = 11;
                      break;
                    case 10:
                      this.__CP__ = 8;
                      this.yield(_7__yield_myu41h_a0a0a0a0a0a51);
                      return true;
                    case 12:
                      this.__CP__ = 1;
                      this.yield(cell);
                      return true;
                    case 0:
                      this.__CP__ = 2;
                      break;
                    case 3:
                      this.__CP__ = 4;
                      break;
                    case 6:
                      this.__CP__ = 7;
                      break;
                    case 9:
                      this.__CP__ = 10;
                      break;
                    case 11:
                      this.__CP__ = 12;
                      break;
                    default:
                      break __loop__;
                  }
                } while (true);
                return false;
              }
            };
          }
        };
      }
    }.invoke(editor.getRootCell());
    Rectangle bounds = check_myu41h_a0b0p(Sequence.fromIterable(leafCells).first());
    if (bounds == null) {
      return new Rectangle();
    }
    for (EditorCell leaf : Sequence.fromIterable(leafCells).skip(1)) {
      if (leaf.getBounds().y == bounds.y) {
        bounds.add(leaf.getBounds());
      } else {
        break;
      }
    }
    return bounds;
  }

  public Bounds getBounds(EditorComponent editor) {
    if (myMessageTarget.getTarget() != MessageTargetEnum.DELETED_CHILD) {
      if (isIndirectRoot(editor)) {
        Rectangle r = getFirstPseudoLineBounds(editor);
        return new Bounds(r.y, r.y + r.height);
      } else {
        return getBoundsSuper(editor);
      }
    } else {
      DeletedNodeMessageTarget cmt = ((DeletedNodeMessageTarget) myMessageTarget);
      EditorCell cell = getCell(editor);
      if (cell == null) {
        return new Bounds(-1, -1);
      }
      if (cmt.getRole().equals(cell.getRole())) {
        if (hasChildrenWithDifferentNode(cell)) {
          return getBoundsForChild((EditorCell_Collection) cell, cmt.getNextChildIndex());
        } else {
          return getBoundsSuper(editor);
        }
      } else {
        int y = (int) cell.getBounds().getMinY();
        return new Bounds(y, y + 1);
      }
    }
  }

  private Bounds getBoundsForChild(EditorCell_Collection cell, int index) {
    int cellIndex = getChildCellIndex(cell, index);
    int lastCellIndex = cell.getChildCount() - 1;

    int minY;
    int maxY;
    if (cellIndex > lastCellIndex) {
      Rectangle lastCellBounds = cell.getChildAt(lastCellIndex).getBounds();
      minY = (isVertical(cell) ?
        (int) lastCellBounds.getMaxY() :
        (int) lastCellBounds.getMinY()
      );
      maxY = Math.max((int) lastCellBounds.getMaxY(), minY + 1);
    } else {
      minY = (int) cell.getChildAt(cellIndex).getBounds().getMinY();
      maxY = (isVertical(cell) ?
        minY + 1 :
        (int) cell.getCellAt(cellIndex).getBounds().getMaxY()
      );
    }
    return new Bounds(minY, maxY);
  }

  private Bounds getBoundsSuper(EditorComponent component) {
    return new Bounds(super.getStart(component), super.getStart(component) + super.getHeight(component));
  }

  @Override
  public int getStart(EditorComponent component) {
    return (int) getBounds(component).start();
  }

  @Override
  public int getHeight(EditorComponent component) {
    return getBounds(component).length();
  }

  public void setHighlighted(boolean highlighted) {
    myHighlighted = highlighted;
  }

  private static boolean hasChildrenWithDifferentNode(EditorCell cell) {
    if (cell instanceof EditorCell_Collection) {
      final EditorCell_Collection collectionCell = (EditorCell_Collection) cell;
      return Sequence.fromIterable(((Iterable<EditorCell>) collectionCell)).any(new IWhereFilter<EditorCell>() {
        public boolean accept(EditorCell child) {
          return child.getSNode() != collectionCell.getSNode();
        }
      });
    } else {
      return false;
    }
  }

  private static boolean isVertical(EditorCell cell) {
    return cell instanceof EditorCell_Collection && (((EditorCell_Collection) cell).getCellLayout() instanceof CellLayout_Vertical || cell.getStyle().get(StyleAttributes.INDENT_LAYOUT_CHILDREN_NEWLINE));
  }

  private static int getChildCellIndex(@NotNull EditorCell_Collection collectionCell, int nodeIndex) {
    if (nodeIndex == -1) {
      return -1;
    }
    int currentNodeIndex = -1;
    for (int i = 0; i < collectionCell.getChildCount(); i++) {
      if (collectionCell.getChildAt(i).getSNode() != collectionCell.getSNode()) {
        currentNodeIndex++;
      }
      if (currentNodeIndex == nodeIndex) {
        return i;
      }
    }
    if (currentNodeIndex == nodeIndex - 1) {
      return collectionCell.getChildCount();
    }
    if (log.isWarnEnabled()) {
      log.warn("Could not find child cell index for deleted child: currentNodeIndex=" + currentNodeIndex + ", total cells=" + collectionCell.getChildCount() + ", requested nodeIndex=" + nodeIndex);
    }
    return -1;
  }

  private static boolean check_myu41h_a0b0h(EditorCell checkedDotOperand) {
    if (null != checkedDotOperand) {
      return checkedDotOperand.isBigCell();
    }
    return false;
  }

  private static boolean check_myu41h_a0a0a8(EditorCell checkedDotOperand) {
    if (null != checkedDotOperand) {
      return checkedDotOperand.isBigCell();
    }
    return false;
  }

  private static ModelAccessor check_myu41h_a0a0a9(EditorCell_Property checkedDotOperand) {
    if (null != checkedDotOperand) {
      return checkedDotOperand.getModelAccessor();
    }
    return null;
  }

  private static SNode check_myu41h_a0a1a9(PropertyAccessor checkedDotOperand) {
    if (null != checkedDotOperand) {
      return checkedDotOperand.getNode();
    }
    return null;
  }

  private static String check_myu41h_a0a0b0j(PropertyAccessor checkedDotOperand) {
    if (null != checkedDotOperand) {
      return checkedDotOperand.getPropertyName();
    }
    return null;
  }

  private static SNode check_myu41h_a0a0a0o(SNode checkedDotOperand, ChangeEditorMessage checkedDotThisExpression) {
    if (null != checkedDotOperand) {
      return checkedDotOperand.getParent();
    }
    return null;
  }

  private static Rectangle check_myu41h_a0b0p(EditorCell checkedDotOperand) {
    if (null != checkedDotOperand) {
      return checkedDotOperand.getBounds();
    }
    return null;
  }

  private static <T> T as_myu41h_a0a0a9(Object o, Class<T> type) {
    return (type.isInstance(o) ?
      (T) o :
      null
    );
  }

  private static <T> T as_myu41h_a0a0a0a0a9(Object o, Class<T> type) {
    return (type.isInstance(o) ?
      (T) o :
      null
    );
  }

  public static interface ConflictChecker {
    public boolean isChangeConflicted(ModelChange change);
  }
}
