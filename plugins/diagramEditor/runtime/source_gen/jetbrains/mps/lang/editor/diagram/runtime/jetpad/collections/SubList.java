package jetbrains.mps.lang.editor.diagram.runtime.jetpad.collections;

/*Generated by MPS */

import jetbrains.jetpad.model.collections.list.ObservableArrayList;
import jetbrains.jetpad.model.collections.list.ObservableList;

public class SubList<ItemT> extends ObservableArrayList<ItemT> {
  private ObservableList<ItemT> myBaseList;

  public SubList(ObservableList<ItemT> baseList) {
    myBaseList = baseList;
  }

  @Override
  protected void afterItemAdded(int index, ItemT item, boolean success) {
    super.afterItemAdded(index, item, success);
    myBaseList.add(item);
  }

  @Override
  protected void afterItemRemoved(int index, ItemT item, boolean success) {
    super.afterItemRemoved(index, item, success);
    myBaseList.remove(item);
  }
}
