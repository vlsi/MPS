<project name="buildPluginDist" default="build" basedir="../">
  <!-- Generated by MPS -->
  <property name="build.dir" location="build" />
  <property name="build.tmp" location="${build.dir}/tmp/buildPluginDist" />
  <property name="build.layout" location="${build.dir}/artifacts/buildPluginDist" />
  <property name="artifacts.buildPlugin" location="${build.dir}/artifacts/buildPlugin" />
  <property file="${artifacts.buildPlugin}/build.properties" prefix="import.buildPlugin" />
  <property name="buildPlugin.plugin.version" value="${import.buildPlugin.buildPlugin.plugin.version}" />
  <property name="buildPlugin.date" value="${import.buildPlugin.buildPlugin.date}" />
  <property name="buildBootstrapCore.version" value="${import.buildPlugin.buildBootstrapCore.version}" />
  
  <target name="assemble" depends="fetchDependencies">
    <mkdir dir="${build.layout}" />
    <zip destfile="${build.layout}/${buildPlugin.plugin.version}-plugin.zip">
      <fileset dir="${artifacts.buildPlugin}" />
    </zip>
    <echo file="${build.layout}/build.properties">buildPlugin.plugin.version=${buildPlugin.plugin.version}${line.separator}buildPlugin.date=${buildPlugin.date}${line.separator}buildBootstrapCore.version=${buildBootstrapCore.version}</echo>
  </target>
  
  <target name="buildDependents">
    <ant antfile="IdeaPlugin/buildPlugin.xml" inheritAll="false" useNativeBasedir="true" />
  </target>
  
  <target name="fetchDependencies" />
  
  <target name="build" depends="assemble" />
  
  <target name="clean">
    <delete dir="${build.tmp}" />
    <delete dir="${build.layout}" />
  </target>
</project>