package jetbrains.mps.make.unittest;

/*Generated by MPS */

import junit.framework.TestCase;
import org.jmock.Mockery;
import org.jmock.integration.junit3.JUnit3Mockery;
import jetbrains.mps.make.runtime.ITarget;
import org.jmock.Expectations;
import junit.framework.Assert;

public class Foobar_Test extends TestCase {
  public void test_foo() throws Exception {
    Mockery m = new JUnit3Mockery();
    final ITarget t = m.mock(ITarget.class);
    m.checking(new Expectations() {
      {
        oneOf(t).getName();
        will(returnValue(new ITarget.Name("foobar")));
      }
    });
    m.checking(new Expectations() {
      {
        never(t).getName();
        will(returnValue(null));
      }
    });
    Assert.assertEquals(new ITarget.Name("foobar"), t.getName());
    m.assertIsSatisfied();
  }
}
